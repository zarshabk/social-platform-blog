import { MetaData } from "next";

export const metadata = {
  title: "about us",
  description: "this is a description about our blogs",
};
const page = () => {
  return <div>about page</div>;
};

export default page;
